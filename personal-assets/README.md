# Setup Project

on root folder run

```bash
python3 -m venv venv
```

On Unix or MacOS

```bash
source venv/bin/activate
```

On Windows, run:

```commandline
venv\Scripts\activate.bat
```

finally

```bash
pip install -r requirements.txt
```